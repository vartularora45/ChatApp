import React from 'react'
import { useState } from 'react'

const useGetMessage = () => {
    const {loading , setLoading} = useState(false);
    const {messages , setMessages , selectedConversation } = use 
  return (
    <div>useGetMessage</div>
  )
}

export default useGetMessage